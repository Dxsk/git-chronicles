name: Security Audit

on:
  push:
    branches: [main]
    paths:
      - 'package*.json'
  schedule:
    - cron: '0 8 * * *'  # Every day at 10:00 Europe/Paris (UTC+2)
  workflow_dispatch:

jobs:
  audit:
    name: npm audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6 (v6.0.2)
      - uses: ./.github/actions/setup

      - name: Run security audit
        run: npm audit --omit=dev
