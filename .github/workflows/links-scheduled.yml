name: Check Links

on:
  push:
    branches: [main]
    paths: ['**.njk', '**.md', 'src/**', '.lychee.toml', '.github/workflows/links-scheduled.yml']
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6 (v6.0.2)
      - uses: ./.github/actions/setup

      - run: npx eleventy

      - uses: lycheeverse/lychee-action@8646ba30535128ac92d33dfc9133794bfdd9b411 # v2 (v2.8.0)
        with:
          args: --no-progress --config .lychee.toml --root-dir _site '_site/**/*.html'
          fail: true
